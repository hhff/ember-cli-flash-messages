import {
  flashMessage
} from 'ember-cli-flash-messages/helpers/flash-message';

module('FlashMessageHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = flashMessage(42);
  ok(result);
});
